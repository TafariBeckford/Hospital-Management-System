<div class="sidebar" id="sidebar">
    <div class="sidebar-inner slimscroll">
        <div id="sidebar-menu" class="sidebar-menu">
            <ul>
                <li class="menu-title">Main</li>
                <li class="active">
                    <a href="@Url.Action("Index", "Home")"><i class="fa fa-dashboard"></i> <span>Dashboard</span></a>
                </li>
                @if (User.IsInRole("Administrator") || User.IsInRole("SuperUser"))
                {
                    <li>
                        <a href="@Url.Action("Staff", "Account")"><i class="fa fa-user-md"></i> <span>Medical Staff</span></a>
                    </li>
                }

                @if (User.IsInRole("Doctor")|| User.IsInRole("SuperUser")|| User.IsInRole("Receptionist"))
                {
                    <li>
                        <a href="@Url.Action("Index", "Patients")"><i class="fa fa-wheelchair"></i> <span>Patients</span></a>
                    </li>
                }
                    @if (User.IsInRole("Administrator"))
                    {
                        <li>
                            <a href="@Url.Action("Index", "Employees")"><i class="fa fa-user-plus"></i> <span>Employees</span></a>
                        </li>
                    }
                    <li>
                        <a href="@Url.Action("Index", "Schedules")"><i class="fa fa-calendar-check-o"></i> <span>Doctor Schedule</span></a>
                    </li>
                    <li>
                        <a href="@Url.Action("Index", "Departments")"><i class="fa fa-hospital-o"></i> <span>Departments</span></a>
                    </li>
                    <li>
                        <a href="@Url.Action("Calendar","Home")"><i class="fa fa-calendar-o"></i> <span>General Calendar</span></a>
                    </li>
                    @if (User.IsInRole("Administrator") || User.IsInRole("SuperUser"))
                    {
                        <li>
                            <a href="@Url.Action("Index","Roles")"><i class="fa fa-cogs"></i> <span>Roles</span></a>
                        </li>
                    }








                </ul>
        </div>
    </div>
</div>
